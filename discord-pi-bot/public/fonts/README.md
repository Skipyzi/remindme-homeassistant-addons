# Local font assets

The harness intentionally works without network fonts. Add licensed local WOFF2 files here and declare them in `styles.css`; the current system fallbacks preserve the layout through Home Assistant ingress.
