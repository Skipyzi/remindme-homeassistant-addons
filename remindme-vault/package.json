{
	"name": "remindme-vault",
	"version": "0.1.0",
	"description": "Editable Obsidian-format vault with a 3D constellation, served from Home Assistant",
	"private": true,
	"scripts": {
		"build": "tsc && node build-frontend.mjs",
		"start": "node dist/server.js"
	},
	"dependencies": {
		"express": "^5.2.1",
		"marked": "^12.0.0",
		"three": "^0.160.0"
	},
	"devDependencies": {
		"@types/express": "^5.0.6",
		"@types/node": "^22.0.0",
		"esbuild": "^0.20.0",
		"typescript": "^5.4.0"
	}
}
